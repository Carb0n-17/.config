# Changelog

## 2026-07-12

Fresh install of Arch Linux + Hyprland

### Added

#### `.gitignore`
```diff
+ Code/
+ dconf/
+ gtk-3.0/
+ mozilla/
+ session/
```

### Changed

#### `hypr/hyprland.lua`
```diff
- hl.config({ autogenerated = true }) -- remove this line to remove the warning
+ hl.config({ autogenerated = false }) -- remove this line to remove the warning

-     output   = ""
+     output   = "eDP-1"

-     mode     = "preferred"
+     mode     = "1920x1080@144"

-     position = "auto"
+     position = "0x0"

-     scale    = "auto"
+     scale    = "1"

+ local polkit = "/usr/lib/hyprpolkitagent/hyprpolkitagent"

+ hl.on("hyprland.start", function()
+   hl.exec_cmd(polkit)
+ end)
```